{
  "name": "worldbnb-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for WorldBNB - Web3 Airbnb on World Chain",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network world",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network worldMainnet",
    "verify": "hardhat verify --network world"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^4.9.0",
    "hardhat": "^2.17.0",
    "dotenv": "^16.3.0"
  },
  "dependencies": {
    "ethers": "^5.7.2"
  }
}
